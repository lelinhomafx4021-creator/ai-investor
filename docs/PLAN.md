# AI 量化投资平台 - 项目规划书

> 一个基于 Spring AI 的智能量化投资平台，支持实时行情推送、AI 智能交易、量化策略回测等功能。

---

## 📌 项目定位

| 项目 | 说明 |
|------|------|
| **类型** | 金融科技（FinTech）- 量化交易模拟平台 |
| **架构** | 单体应用（后期可拆微服务） |
| **目标** | 大三实习面试项目 |
| **核心亮点** | 高并发 + AI |

**一句话介绍**：
> 一个模拟炒股平台，用户可以用虚拟资金交易，AI 助手能帮你查行情、下单、生成策略。

---

## 🎯 核心亮点

| 亮点 | 技术点 | 面试价值 |
|------|--------|----------|
|  **实时行情推送** | WebSocket + 定时任务 | ⭐⭐⭐⭐⭐ |
| 🤖 **AI 智能交易** | Spring AI + Function Calling | ⭐⭐⭐⭐⭐ |
| � **RAG 知识问答** | 向量检索 + 知识库 | ⭐⭐⭐⭐ |
| ⚡ **高并发订单处理** | MQ 削峰 + 分布式锁 | ⭐⭐⭐⭐ |
| �️ **限流熔断** | Sentinel | ⭐⭐⭐⭐ |
| � **策略回测** | 历史数据回放 | ⭐⭐⭐ |

---

## 🏗️ 系统架构（单体）

```
┌─────────────────────────────────────────────────────────────────┐
│                         前端                                     │
│  ┌─────────────────────────┐  ┌─────────────────────────────┐   │
│  │       用户端            │  │         管理端              │   │
│  │  • 行情大屏(WebSocket)  │  │  • 股票管理                 │   │
│  │  • 模拟交易             │  │  • 用户管理                 │   │
│  │  • 我的持仓             │  │  • 知识库管理               │   │
│  │  • AI 助手对话          │  │  • 系统配置                 │   │
│  └─────────────────────────┘  └─────────────────────────────┘   │
└────────────────────────────────┬────────────────────────────────┘
                                 │
                       ┌─────────▼─────────┐
                       │   Nginx + Sentinel│
                       │      限流         │
                       └─────────┬─────────┘
                                 │
┌────────────────────────────────▼────────────────────────────────┐
│                    Spring Boot 单体应用                          │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                      Controller 层                          │ │
│  │  /api/user/**  用户端接口    /api/admin/** 管理端接口       │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐            │
│  │   行情服务    │ │   交易服务    │ │   AI 服务    │            │
│  │              │ │              │ │              │            │
│  │ • 价格模拟   │ │ • 买入/卖出  │ │ • 对话问答   │            │
│  │ • WS 推送    │ │ • 持仓管理   │ │ • RAG 检索   │            │
│  │ • K线计算    │ │ • 分布式锁   │ │ • 工具调用   │            │
│  └──────────────┘ └──────────────┘ └──────────────┘            │
│                                                                  │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐            │
│  │   策略服务    │ │  知识库服务   │ │   用户服务    │            │
│  │              │ │              │ │              │            │
│  │ • 策略管理   │ │ • 文档管理   │ │ • 登录注册   │            │
│  │ • 回测引擎   │ │ • 向量化     │ │ • 资金管理   │            │
│  └──────────────┘ └──────────────┘ └──────────────┘            │
└────────────────────────────────┬────────────────────────────────┘
                                 │
        ┌────────────────────────┼────────────────────────┐
        │                        │                        │
        ▼                        ▼                        ▼
┌──────────────┐        ┌──────────────┐        ┌──────────────┐
│    MySQL     │        │    Redis     │        │   RabbitMQ   │
│              │        │              │        │              │
│ • 用户数据   │        │ • 实时行情   │        │ • 订单队列   │
│ • 交易记录   │        │ • 分布式锁   │        │ • 异步处理   │
│ • 策略配置   │        │ • 向量存储   │        │              │
└──────────────┘        └──────────────┘        └──────────────┘
```

---

## 👤 用户端功能

### 1. 实时行情

| 功能 | 说明 | 技术点 |
|------|------|--------|
| 股票列表 | 查看所有股票和价格 | 定时刷新/WebSocket |
| 实时推送 | 价格实时变动 | **WebSocket** |
| K线图 | 查看历史走势 | 前端图表库 |

**技术流程**：
```
定时任务(3秒) → 模拟价格波动 → 更新 Redis → WebSocket 推送 → 浏览器更新
```

### 2. 模拟交易

| 功能 | 说明 | 技术点 |
|------|------|--------|
| 买入股票 | 输入数量下单 | 分布式锁、幂等性 |
| 卖出股票 | 卖出持仓 | 事务处理 |
| 当前委托 | 未成交订单 | - |
| 交易记录 | 历史成交 | 分页查询 |

**异常处理**：
- 余额不足 → 提示最多能买多少股
- 持仓不足 → 提示当前持仓数量
- 非交易时间 → 提示开盘时间

### 3. 我的资产

| 功能 | 说明 |
|------|------|
| 持仓列表 | 持有的股票、数量、盈亏 |
| 资产总览 | 总资产、可用余额、持仓市值 |
| 收益统计 | 今日盈亏、总收益率 |

### 4. AI 助手 ⭐核心

| 功能 | 说明 | 技术点 |
|------|------|--------|
| 智能问答 | 问投资知识 | **RAG 知识检索** |
| 查询股票 | "茅台多少钱" | **Function Calling** |
| 查询持仓 | "我的持仓怎么样" | **Function Calling** |
| AI 下单 | "帮我买100股茅台" | **Function Calling** |
| 推荐股票 | "推荐买什么" | **Function Calling** |
| 策略生成 | "帮我写个均线策略" | **Spring AI** |

**对话示例**：
```
用户：帮我买100股茅台
AI：您确定买入贵州茅台100股吗？预计花费18.5万，请回复"确认"

用户：确认
AI：已买入贵州茅台100股，成交价1856元，共185600元，剩余余额81.4万

用户：什么是市盈率？
AI：（检索知识库）市盈率是股价与每股收益的比值，用来衡量股票估值...

用户：余额不够了怎么办
AI：您可以卖出部分持仓，或者等收益增加后再买入
```

### 5. 量化策略（可选）

| 功能 | 说明 |
|------|------|
| 我的策略 | 查看已创建的策略 |
| 创建策略 | 设置买卖规则 |
| AI 生成策略 | 自然语言描述，AI 生成 |
| 策略回测 | 用历史数据测试 |

### 6. 排行榜

| 功能 | 说明 | 技术点 |
|------|------|--------|
| 收益排行 | 按收益率排名 | **Redis ZSet** |
| 日榜/周榜/月榜 | 不同时间维度 | 定时统计 |

---

## � 管理端功能

### 1. 股票管理

| 功能 | 说明 |
|------|------|
| 股票列表 | 增删改查 |
| 批量导入 | Excel 导入 |
| 行情配置 | 波动幅度、推送频率 |

### 2. 用户管理

| 功能 | 说明 |
|------|------|
| 用户列表 | 查看所有用户 |
| 资金调整 | 给用户加虚拟资金 |
| 封禁用户 | 异常处理 |

### 3. 知识库管理 ⭐

| 功能 | 说明 | 技术点 |
|------|------|--------|
| 文档列表 | 已上传的文档 | - |
| 上传文档 | PDF/TXT/MD | 文档解析 |
| 手动录入 | 直接输入内容 | - |
| 向量化 | 文档转向量存储 | **Embedding** |

**流程**：
```
管理员上传文档 → 文档分块(500字) → 调用 Embedding API → 存入向量库
                                                            ↓
用户提问 → 问题向量化 → 向量相似度检索 → 取 Top3 相关文档 → 拼入 Prompt → AI 回答
```

**知识库内容**：
- 投资基础：股票、基金、市盈率、K线等
- 量化教程：均线策略、MACD 策略等
- 平台帮助：如何下单、如何回测
- 风险提示：投资有风险

### 4. 系统配置

| 功能 | 说明 |
|------|------|
| 交易规则 | 交易时间、涨跌停 |
| 新用户资金 | 默认发放金额 |

---

## � 权限控制

### 技术方案

| 技术 | 说明 |
|------|------|
| Spring Security | 认证授权框架 |
| JWT | 无状态 Token，存 Redis 可踢人 |
| BCrypt | 密码加密 |

### 接口权限划分

| 路径 | 权限 | 说明 |
|------|------|------|
| `/api/auth/**` | 公开 | 登录、注册 |
| `/api/user/**` | 需登录 | 用户端接口 |
| `/api/admin/**` | 需 ADMIN 角色 | 管理端接口 |

### 认证流程

```
1. 登录
   用户名+密码 → AuthenticationManager 验证 → 生成 JWT → 返回给前端

2. 访问接口
   请求带 Authorization: Bearer xxx → JwtAuthFilter 解析 → 验证有效 → 放入 SecurityContext → 进入 Controller

3. 权限校验
   SecurityConfig 配置：
   - /api/auth/** → permitAll()
   - /api/admin/** → hasRole("ADMIN")
   - anyRequest() → authenticated()
```

### 核心代码示例

```java
// SecurityConfig.java - 安全配置
@Configuration
@EnableWebSecurity
public class SecurityConfig {

    @Autowired
    private JwtAuthFilter jwtAuthFilter;

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .csrf(csrf -> csrf.disable())
            .sessionManagement(session -> 
                session.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/api/auth/**").permitAll()
                .requestMatchers("/api/admin/**").hasRole("ADMIN")
                .anyRequest().authenticated()
            )
            .addFilterBefore(jwtAuthFilter, 
                UsernamePasswordAuthenticationFilter.class);
        
        return http.build();
    }

    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}
```

```java
// 登录接口
@PostMapping("/api/auth/login")
public Result login(@RequestBody LoginDto dto) {
    // 验证账号密码
    Authentication auth = authManager.authenticate(
        new UsernamePasswordAuthenticationToken(dto.getUsername(), dto.getPassword()));
    
    // 生成 JWT
    String token = jwtUtil.generateToken(auth.getName());
    
    return Result.success(token);
}
```

---

## ⚠️ 全局异常处理

### 统一返回格式

```java
// Result.java
public class Result<T> {
    private Integer code;    // 状态码
    private String msg;      // 提示信息
    private T data;          // 数据

    public static <T> Result<T> success(T data) {
        return new Result<>(200, "成功", data);
    }

    public static Result<?> fail(Integer code, String msg) {
        return new Result<>(code, msg, null);
    }
}
```

### 状态码定义

| 状态码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 参数错误 |
| 401 | 未登录 |
| 403 | 无权限 |
| 500 | 系统错误 |
| 1001 | 余额不足 |
| 1002 | 持仓不足 |
| 1003 | 非交易时间 |

### 全局异常处理器

```java
@RestControllerAdvice
public class GlobalExceptionHandler {

    // 业务异常
    @ExceptionHandler(BizException.class)
    public Result<?> handleBiz(BizException e) {
        return Result.fail(e.getCode(), e.getMsg());
    }

    // 未登录
    @ExceptionHandler(NotLoginException.class)
    public Result<?> handleNotLogin(NotLoginException e) {
        return Result.fail(401, "请先登录");
    }

    // 无权限
    @ExceptionHandler(AccessDeniedException.class)
    public Result<?> handleAccessDenied(AccessDeniedException e) {
        return Result.fail(403, "无权限访问");
    }

    // 参数校验失败
    @ExceptionHandler(MethodArgumentNotValidException.class)
    public Result<?> handleValidation(MethodArgumentNotValidException e) {
        String msg = e.getBindingResult().getFieldError().getDefaultMessage();
        return Result.fail(400, msg);
    }

    // 兜底
    @ExceptionHandler(Exception.class)
    public Result<?> handleException(Exception e) {
        log.error("系统异常", e);
        return Result.fail(500, "系统繁忙，请稍后重试");
    }
}
```

### 自定义业务异常

```java
public class BizException extends RuntimeException {
    private Integer code;
    private String msg;

    public BizException(Integer code, String msg) {
        this.code = code;
        this.msg = msg;
    }

    // 快捷方法
    public static BizException of(String msg) {
        return new BizException(400, msg);
    }
}

// 使用
if (user.getBalance() < totalPrice) {
    throw BizException.of("余额不足，当前余额" + user.getBalance());
}
```

---

## 📖 接口文档

### 技术方案

| 技术 | 说明 |
|------|------|
| Knife4j | Swagger 的增强版，UI 更好看 |
| 访问地址 | `http://localhost:8080/doc.html` |

### 配置

```java
@Configuration
public class Knife4jConfig {

    @Bean
    public OpenAPI openAPI() {
        return new OpenAPI()
            .info(new Info()
                .title("AI 量化投资平台 API")
                .version("1.0")
                .description("接口文档"));
    }
}
```

### 接口文档注解

```java
@RestController
@RequestMapping("/api/user/trade")
@Tag(name = "交易接口", description = "买入卖出相关")
public class TradeController {

    @Operation(summary = "买入股票", description = "扣除余额，增加持仓")
    @PostMapping("/buy")
    public Result<?> buy(
        @Parameter(description = "股票代码") @RequestParam String stockCode,
        @Parameter(description = "数量") @RequestParam Integer quantity) {
        // ...
    }
}
```

---

## �📊 数据方案

### 股票数据来源

**开发阶段：模拟数据**
```java
// 1. 初始化30只热门股票（真实代码和名称，假价格）
// 2. 定时任务每3秒模拟价格波动（-2% ~ +2%）
@Scheduled(fixedRate = 3000)
public void mockPriceChange() {
    for (Stock stock : stocks) {
        double change = (Math.random() - 0.5) * 0.04;
        stock.setPrice(stock.getPrice() * (1 + change));
    }
    // 推送给所有 WebSocket 客户端
    wsHandler.pushPrices(stocks);
}
```

**面试时说**：
> "项目用模拟数据演示，重点是技术实现。接入真实数据只需替换数据源即可。"

---

## 🤖 AI 工具定义

```java
// 查询股票
@Tool(description = "查询股票实时价格")
public String queryStock(@Param("stockCode") String stockCode);

// 查询持仓
@Tool(description = "查询用户当前持仓和盈亏")
public String queryHolding();

// 买入股票
@Tool(description = "帮用户买入股票，返回成交结果或失败原因")
public String buyStock(
    @Param("stockCode") String stockCode,
    @Param("quantity") Integer quantity
);

// 卖出股票
@Tool(description = "帮用户卖出股票，quantity=0表示全部卖出")
public String sellStock(
    @Param("stockCode") String stockCode,
    @Param("quantity") Integer quantity
);

// 推荐股票
@Tool(description = "根据用户风险偏好推荐股票组合")
public String recommendStocks(@Param("riskLevel") String riskLevel);

// 生成策略
@Tool(description = "根据用户描述生成量化策略配置")
public String generateStrategy(@Param("description") String description);
```

---

## 🗓️ 开发计划（25天）

### 第一阶段：基础搭建（Day 1-5）

| 天 | 任务 |
|----|------|
| 1 | 项目初始化、依赖配置、Docker Compose |
| 2 | 数据库设计、MyBatis-Plus 代码生成 |
| 3 | 用户模块：注册、登录、JWT |
| 4 | Redis 配置、分布式锁封装 |
| 5 | Sentinel 限流配置 |

### 第二阶段：行情系统（Day 6-10）

| 天 | 任务 |
|----|------|
| 6 | 股票管理（管理端 CRUD） |
| 7 | 模拟行情：定时任务 + 价格波动 |
| 8 | **WebSocket 实时推送** |
| 9 | 前端行情页面 |
| 10 | K线数据计算（可选） |

### 第三阶段：交易系统（Day 11-15）

| 天 | 任务 |
|----|------|
| 11 | 买入接口（余额校验、扣款、加持仓） |
| 12 | 卖出接口（持仓校验、加款、减持仓） |
| 13 | **分布式锁 + 幂等性** |
| 14 | 持仓列表、盈亏计算 |
| 15 | 交易记录、前端交易页 |

### 第四阶段：AI 功能（Day 16-21）⭐核心

| 天 | 任务 |
|----|------|
| 16 | Spring AI 集成、基础对话 |
| 17 | **知识库管理（管理端上传文档）** |
| 18 | **RAG 检索实现** |
| 19 | **Function Calling（查股票、查持仓）** |
| 20 | **Function Calling（买入、卖出）** |
| 21 | AI 对话页面、流式输出 |

### 第五阶段：完善优化（Day 22-25）

| 天 | 任务 |
|----|------|
| 22 | 排行榜（Redis ZSet） |
| 23 | 策略回测（可选） |
| 24 | 前端完善、Bug 修复 |
| 25 | 文档整理、面试准备 |

---

## 📦 技术栈

| 分类 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 语言 | Java | 17 | LTS 版本 |
| 框架 | Spring Boot | 3.4.x | 主框架 |
| AI | Spring AI | 1.0.x | AI 能力集成 |
| 权限控制 | Spring Security | 6.x | 认证授权 |
| JWT | jjwt | 0.12.x | 无状态 Token |
| 数据库 | MySQL | 8.0 | 主数据库 |
| ORM | MyBatis-Plus | 3.5.x | 简化开发 |
| 缓存 | Redis | 7.x | 缓存 + 向量存储 |
| 分布式锁 | Redisson | 3.x | 并发控制 |
| 消息队列 | RabbitMQ | 3.x | 削峰、解耦 |
| 限流 | Sentinel | 1.8.x | 限流熔断 |
| 实时通信 | WebSocket | - | 行情推送 |
| 接口文档 | Knife4j | 4.x | Swagger 增强 |
| 前端 | Vue 3 | - | 用户界面 |
| 部署 | Docker Compose | - | 容器化 |

---

## 📁 项目结构

```
ai-investor/
├── src/main/java/com/investor/
│   ├── config/               # 配置类
│   │   ├── SecurityConfig    # Spring Security 配置
│   │   ├── WebSocketConfig   # WebSocket 配置
│   │   ├── RedisConfig       # Redis + Redisson 配置
│   │   ├── AiConfig          # Spring AI 配置
│   │   ├── SentinelConfig    # Sentinel 限流配置
│   │   ├── RabbitConfig      # RabbitMQ 配置
│   │   └── Knife4jConfig     # 接口文档配置
│   ├── controller/
│   │   ├── AuthController    # 登录注册（公开）
│   │   ├── user/             # 用户端接口（需登录）
│   │   │   ├── StockController
│   │   │   ├── TradeController
│   │   │   ├── HoldingController
│   │   │   └── ChatController
│   │   └── admin/            # 管理端接口（需 ADMIN 角色）
│   │       ├── StockAdminController
│   │       ├── UserAdminController
│   │       └── KnowledgeController
│   ├── security/             # 安全相关
│   │   ├── JwtAuthFilter     # JWT 过滤器
│   │   ├── JwtUtil           # JWT 工具类
│   │   └── UserDetailsServiceImpl  # 加载用户
│   ├── service/
│   │   ├── UserService       # 用户服务
│   │   ├── StockService      # 股票服务
│   │   ├── TradeService      # 交易服务
│   │   ├── HoldingService    # 持仓服务
│   │   ├── ChatService       # AI 对话服务
│   │   └── RagService        # RAG 检索服务
│   ├── websocket/            # WebSocket 处理
│   │   └── MarketWebSocketHandler
│   ├── tools/                # AI 工具（Function Calling）
│   │   ├── StockQueryTool    # 查询股票
│   │   ├── HoldingQueryTool  # 查询持仓
│   │   └── TradeTool         # 执行交易
│   ├── task/                 # 定时任务
│   │   └── MarketPriceTask   # 模拟价格波动
│   ├── exception/            # 异常处理
│   │   ├── GlobalExceptionHandler  # 全局异常
│   │   └── BizException      # 业务异常
│   ├── common/               # 通用类
│   │   ├── Result            # 统一返回格式
│   │   └── ResultCode        # 状态码枚举
│   ├── mapper/
│   ├── entity/
│   ├── dto/
│   └── vo/
├── src/main/resources/
│   ├── application.yml
│   ├── application-dev.yml   # 开发环境
│   ├── application-prod.yml  # 生产环境
│   └── prompts/
│       └── advisor.st        # AI Prompt 模板
├── docs/
│   └── PLAN.md               # 项目规划（本文档）
└── docker-compose.yml
```

---

## 🎤 面试话术

### 项目介绍（1分钟）

> "这是一个 AI 量化投资平台，主要有三个亮点：
>
> 1. **高并发行情推送**：用 WebSocket 实现实时行情，服务端模拟价格波动推送给客户端
>
> 2. **AI 智能交易**：不只是聊天机器人，AI 可以通过 Function Calling 调用后端接口，帮用户查行情、分析持仓、甚至执行买卖
>
> 3. **RAG 知识问答**：把投资知识文档向量化存入知识库，用户问问题时先检索相关文档，让 AI 的回答有据可查
>
> 技术栈用的 Spring Boot 3.4 + Spring AI + Redis + WebSocket，单体架构但按模块化设计，方便后期扩展。"

### 为什么做这个项目？

> "1. AI 是当前热点，想学习 Spring AI 做个实战
> 2. 量化交易有真实的高并发场景，比普通 CRUD 项目有技术深度
> 3. 想做点和大多数人不一样的项目"

---

## 💡 后期可扩展

| 方向 | 说明 |
|------|------|
| 拆微服务 | 用户服务、交易服务、行情服务、AI 服务 |
| 真实数据 | 接入 Python 数据服务获取真实行情 |
| 自动交易 | 策略自动执行，满足条件自动下单 |
| 更多AI能力 | 市场情绪分析、投资组合优化 |

---

**开始干吧！🚀**
